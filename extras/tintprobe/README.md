# Tintprobe compatibility exports

Generated from `scripts/palette/ithilien.json` by `scripts/generate_themes.py`.
Do not edit these files by hand. The pinned Tintprobe release reads one resolved
role file per variant; `tintprobe.json` points here. Both variants are authored
only in the single canonical JSON.
